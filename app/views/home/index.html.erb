<% content_for :main_content do %>

  <div class="flex flex-col md:flex-row">
    <div class="w-full md:w-3/4 p-4">
      <h1 class="text-3xl font-bold mb-2">Dashboard</h1>
      <div class="flex flex-wrap -mx-2">
        <%= render 'shared/card', title: 'Balance', subtitle: 'See your balance', options: { msg: "R#{number_to_currency(current_account.balance)}" } %>
        <%= render 'shared/card', title: 'Bank Statement', subtitle: 'See all your statement', options: { link_path: statement_path } %>
      </div>
    </div>
  </div>

<% end %>

<%= render 'templates/sidebar_template' %>