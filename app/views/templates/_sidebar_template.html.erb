<div class="drawer lg:drawer-open">
  <input id="my-drawer-cl" type="checkbox" class="drawer-toggle" />
  <div class="drawer-content flex flex-col">
    <label for="my-drawer-cl" class="btn btn-primary drawer-button lg:hidden">
      Open Menu
    </label>
    
    <!-- Page content here -->
    <div class="p-4">
      <%= yield :main_content %>
    </div>
  </div>


  <div class="drawer-side">
    <label for="my-drawer-cl" aria-label="close sidebar" class="drawer-overlay"></label>
    <ul class="menu bg-base-200 text-base-content min-h-full w-80 p-4 flex flex-col justify-between">
      <!-- TOP -->
      <div>
        <a href="<%= root_path %>" class="text-2xl font-bold text-center mb-4 block">
          <%= current_account.name.capitalize %>'s Space
        </a>

        <button class="btn btn-primary w-full m-2">Saque</button> 
        <button class="btn btn-secondary w-full m-2">Deposito</button>
        <button class="btn btn-primary w-full m-2">Extrato</button>

        <%= yield :sidebar_content %>
      </div>
      <!-- TOP -->

      <!-- Bottom -->
      <div class="mt-auto flex flex-col justify-center w-full">
        <!-- Settings -->
        <div class="dropdown dropdown-top dropdown-end w-full">
          <div tabindex="0" role="button" class="btn btn-primary w-full">Settings</div>
          <ul tabindex="0" class="dropdown-content menu bg-base-100 rounded-box z-[1] w-full p-2 shadow">
            <li><%= link_to "Log out", session_path(current_account), data: { turbo_method: :delete } %></li>
          </ul>
        </div>
        <!-- Settings -->
      </div>
      <!-- Bottom -->

    </ul>
  </div>
</div>
